{
  "name": "pixiejs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:build": "tsc --noEmit --strict",
    "test:build:watch": "tsc --noEmit --strict --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "@pixi/ui": "^2.1.5",
    "fastpriorityqueue": "^0.7.5",
    "pixi.js": "^8.3.4"
  }
}
